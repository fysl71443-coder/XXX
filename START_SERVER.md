# دليل تشغيل السيرفر
## تاريخ: 2026-01-20

---

## 🚀 كيفية تشغيل السيرفر

### الطريقة 1: تشغيل مباشر
```bash
cd backend
node server.js
```

### الطريقة 2: تشغيل في وضع التطوير (مع إعادة التشغيل التلقائي)
```bash
cd backend
npm run dev
```

### الطريقة 3: تشغيل في الخلفية (Windows PowerShell)
```powershell
cd backend
Start-Process node -ArgumentList "server.js" -WindowStyle Hidden
```

---

## ⚙️ المتطلبات

1. **Node.js**: يجب أن يكون الإصدار >= 22.12.0
2. **قاعدة البيانات**: يجب أن يكون PostgreSQL متاحاً ومتصل
3. **متغيرات البيئة**: يجب أن يكون ملف `.env` موجوداً في مجلد `backend`

---

## 🔍 التحقق من حالة السيرفر

### التحقق من المنفذ:
```bash
# Windows
netstat -ano | findstr ":5000"
netstat -ano | findstr ":4000"

# Linux/Mac
lsof -i :5000
lsof -i :4000
```

### التحقق من Health Endpoint:
```bash
curl http://localhost:5000/api/health
curl http://localhost:4000/api/health
```

---

## 🐛 حل المشاكل الشائعة

### 1. ERR_CONNECTION_REFUSED
**السبب**: السيرفر غير يعمل
**الحل**: 
- تأكد من تشغيل السيرفر
- تحقق من المنفذ المستخدم (5000 أو 4000)
- تحقق من أن قاعدة البيانات متصلة

### 2. Database Connection Error
**السبب**: قاعدة البيانات غير متصلة
**الحل**:
- تحقق من `DATABASE_URL` في ملف `.env`
- تأكد من أن PostgreSQL يعمل
- تحقق من بيانات الاتصال

### 3. Port Already in Use
**السبب**: المنفذ مستخدم من قبل عملية أخرى
**الحل**:
```bash
# Windows - إيجاد العملية التي تستخدم المنفذ
netstat -ano | findstr ":5000"
# ثم إنهاء العملية
taskkill /PID <PID> /F
```

---

## 📝 ملاحظات

- السيرفر يعمل على المنفذ **5000** في وضع التطوير (dev)
- السيرفر يعمل على المنفذ **4000** افتراضياً
- يمكن تغيير المنفذ عبر متغير البيئة `PORT`

---

**آخر تحديث:** 2026-01-20
