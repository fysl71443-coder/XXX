# Playwright E2E Tests - Full Workflow Simulation

## الوصف
اختبارات E2E شاملة لمحاكاة تدفق العمل الكامل للمستخدم الحقيقي في جميع وحدات النظام ERP.

## الهدف من الاختبارات

الاختبارات لا تكتفي بفتح الشاشات فقط، بل تحاكي **تدفق العمل الكامل**:

### ✅ ما يتم اختباره:

1. **الوصول للشاشة** - فتح الشاشة بنجاح
2. **التفاعلات الأساسية**:
   - الضغط على الأزرار (Create, Update, Delete, PDF, Excel)
   - إدخال البيانات في الفورمات
   - اختيار فلاتر وتواريخ
   - النقر على القوائم والجداول
3. **التحقق من البيانات**:
   - مقارنة البيانات المعروضة مع قاعدة البيانات
   - التحقق من التوازن المحاسبي (Debit = Credit)
   - التحقق من صحة الحسابات والإجماليات
4. **الأداء**:
   - قياس سرعة تحميل الصفحات
   - التحقق من عدم تجمد الواجهة
5. **الأخطاء**:
   - أي خطأ في Console = فشل فوري
   - أي خطأ في Network (404, 500) = فشل فوري
   - CORS errors = فشل فوري

## أنواع الاختبارات

### 1. الاختبارات الأساسية (`*-spec.js`)
- فتح الشاشة والتحقق من تحميلها
- التحقق من العناصر الأساسية

### 2. الاختبارات الشاملة (`*-full.spec.js`)
- تدفق العمل الكامل
- التفاعلات الكاملة
- التحقق من البيانات مع قاعدة البيانات
- اختبار الفلاتر والأزرار
- التحقق من الأداء

## المتطلبات

1. **تثبيت Playwright:**
```bash
npm install --save-dev @playwright/test playwright
npx playwright install chromium
```

2. **تأكد من تشغيل السيرفر:**
```bash
npm run dev
```

3. **تأكد من وجود مستخدم admin:**
- Email: `admin@local.test`
- Password: `admin123`

## الاستخدام

### تشغيل الاختبارات الأساسية:
```bash
npm run test:playwright
```

### تشغيل الاختبارات الشاملة (Full Workflow):
```bash
npx playwright test --project=chromium-full-workflow
```

### تشغيل اختبار محدد:
```bash
npx playwright test tests/02-accounting-full.spec.js
```

### تشغيل مع واجهة Playwright UI:
```bash
npm run test:playwright:ui
```

## الاختبارات المتاحة

### الاختبارات الأساسية:
1. **01-login.spec.js** - تسجيل الدخول
2. **02-accounting.spec.js** - المحاسبة (بسيط)
3. **03-customers.spec.js** - العملاء (بسيط)
4. **04-suppliers.spec.js** - الموردين (بسيط)
5. **05-employees.spec.js** - الموظفين (بسيط)
6. **06-pos.spec.js** - نقطة البيع (بسيط)
7. **07-reports.spec.js** - التقارير (بسيط)

### الاختبارات الشاملة (Full Workflow):
1. **02-accounting-full.spec.js** - المحاسبة الكاملة:
   - شجرة الحسابات
   - القيود اليومية
   - الفلاتر (تاريخ، حالة، حساب)
   - التحقق من التوازن (Debit = Credit)
   - اختبار الأزرار (Create, PDF, Excel)
   - التحقق من الأداء

2. **03-customers-full.spec.js** - العملاء الكاملة:
   - فتح صفحة العملاء
   - اختبار الفلاتر والبحث
   - إنشاء عميل جديد
   - التحقق من البيانات المعروضة
   - اختبار التصدير (PDF, Excel)
   - تنظيف البيانات المُنشأة

3. **04-suppliers-full.spec.js** - الموردين الكاملة:
   - فتح صفحة الموردين
   - اختبار الفلاتر
   - التحقق من البطاقات والفواتير
   - التحقق من البيانات
   - اختبار التصدير

4. **05-employees-full.spec.js** - الموظفين الكاملة:
   - فتح صفحة الموظفين
   - التحقق من الإحصائيات
   - اختبار إنشاء موظف
   - اختبار البطاقات
   - اختبار التصدير

5. **06-pos-full.spec.js** - نقطة البيع الكاملة:
   - فتح صفحة POS
   - اختيار فرع
   - التحقق من شاشة الفاتورة
   - التحقق من حساب الإجمالي
   - التحقق من الأداء

6. **07-reports-full.spec.js** - التقارير الكاملة:
   - فتح صفحة التقارير
   - فتح تقرير ميزان المراجعة
   - التحقق من محتوى التقرير
   - التحقق من التوازن من قاعدة البيانات
   - اختبار فلاتر التقرير
   - اختبار تصدير التقرير

## السلوك الصارم

الاختبارات تفشل فوراً عند:
- ❌ أي خطأ حرج في Console (Uncaught, ReferenceError, TypeError)
- ❌ أي خطأ في API (400, 500)
- ❌ عدم توازن محاسبي (Debit ≠ Credit)
- ❌ أي زر لا يستجيب
- ❌ أي صفحة لا تحمل بشكل صحيح
- ❌ CORS errors
- ⚠️ تحميل بطيء (> 10 ثواني)

## التحقق من البيانات

الاختبارات الشاملة تقوم بـ:
- ✅ الاتصال بقاعدة البيانات PostgreSQL
- ✅ مقارنة البيانات المعروضة مع قاعدة البيانات
- ✅ التحقق من التوازن المحاسبي
- ✅ التحقق من صحة الحسابات

## المخرجات

- ✅ رسائل نجاح تفصيلية لكل خطوة
- ❌ رسائل خطأ واضحة مع تفاصيل
- 📊 قياسات الأداء
- 📸 لقطات شاشة عند الفشل
- 📄 تقرير HTML تفصيلي

## ملاحظات

- الاختبارات الشاملة تستخدم قاعدة البيانات للتحقق من البيانات
- يتم تنظيف البيانات المُنشأة تلقائياً بعد الاختبار
- الاختبارات تعمل بشكل تسلسلي لمحاكاة المستخدم الحقيقي
- Chrome الحقيقي (غير Headless) لمشاهدة التنفيذ
