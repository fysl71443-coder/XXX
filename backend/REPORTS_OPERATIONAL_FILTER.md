# فلترة التقارير التشغيلية (Operational Reports Filter)

**التاريخ:** 2026-01-22

---

## 📋 الوصف (Description)

تمت إضافة فلترة اختيارية في التقارير لاستبعاد قيود الأرصدة الافتتاحية (`opening` entries) عند الحاجة لتقارير تشغيلية فقط.

---

## ✅ التقارير المدعومة (Supported Reports)

### 1. Trial Balance (`/api/reports/trial-balance`)

**الاستخدام:**
```
GET /api/reports/trial-balance?from=2026-01-01&to=2026-12-31&exclude_opening=true
```

**أو:**
```
GET /api/reports/trial-balance?from=2026-01-01&to=2026-12-31&operational_only=true
```

**النتيجة:**
- ✅ يستبعد جميع القيود من نوع `reference_type = 'opening'`
- ✅ يعرض فقط القيود التشغيلية (فواتير، مصروفات، رواتب، قيود يدوية)

---

### 2. Sales vs Expenses (`/api/reports/sales-vs-expenses`)

**الاستخدام:**
```
GET /api/reports/sales-vs-expenses?from=2026-01-01&to=2026-12-31&exclude_opening=true
```

**النتيجة:**
- ✅ يستبعد قيود الأرصدة الافتتاحية من المبيعات والمصروفات
- ✅ يعرض فقط المبيعات والمصروفات التشغيلية

---

### 3. Sales by Branch (`/api/reports/sales-by-branch`)

**الاستخدام:**
```
GET /api/reports/sales-by-branch?from=2026-01-01&to=2026-12-31&exclude_opening=true
```

**النتيجة:**
- ✅ يستبعد قيود الأرصدة الافتتاحية من المبيعات
- ✅ يعرض فقط المبيعات التشغيلية حسب الفرع

---

### 4. Account Journal (`/api/journal/account/:id`)

**الاستخدام:**
```
GET /api/journal/account/123?from=2026-01-01&to=2026-12-31&exclude_opening=true
```

**النتيجة:**
- ✅ يستبعد قيود الأرصدة الافتتاحية من كشف الحساب
- ✅ يعرض فقط الحركات التشغيلية للحساب

---

## 🔧 المعاملات (Parameters)

### `exclude_opening` (boolean)

- **القيمة:** `true` أو `false`
- **الافتراضي:** `false` (لا يستبعد)
- **الوصف:** يستبعد قيود الأرصدة الافتتاحية من التقرير

### `operational_only` (boolean)

- **القيمة:** `true` أو `false`
- **الافتراضي:** `false` (لا يستبعد)
- **الوصف:** نفس وظيفة `exclude_opening` (اسم بديل)

**ملاحظة:** يمكن استخدام أي من المعاملين (`exclude_opening` أو `operational_only`) - كلاهما يؤدي نفس الوظيفة.

---

## 📝 أمثلة الاستخدام (Usage Examples)

### مثال 1: تقرير الميزانية العمومية (Trial Balance) بدون أرصدة افتتاحية

```bash
curl -X GET "http://localhost:5000/api/reports/trial-balance?from=2026-01-01&to=2026-12-31&exclude_opening=true" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### مثال 2: تقرير المبيعات مقابل المصروفات (تشغيلي فقط)

```bash
curl -X GET "http://localhost:5000/api/reports/sales-vs-expenses?from=2026-01-01&to=2026-12-31&operational_only=true" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### مثال 3: كشف حساب بدون أرصدة افتتاحية

```bash
curl -X GET "http://localhost:5000/api/journal/account/123?from=2026-01-01&to=2026-12-31&exclude_opening=true" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## ⚠️ ملاحظات مهمة (Important Notes)

### 1. الفلترة اختيارية

- ✅ **الافتراضي:** جميع القيود تظهر (بما في ذلك الأرصدة الافتتاحية)
- ✅ **عند التفعيل:** يتم استبعاد قيود الأرصدة الافتتاحية فقط

### 2. لا يؤثر على البيانات الأساسية

- ✅ القيود لا يتم حذفها من قاعدة البيانات
- ✅ الفلترة فقط في الاستعلامات
- ✅ يمكن الوصول للبيانات الكاملة بدون المعامل

### 3. متوافق مع الفلترة الأخرى

- ✅ يعمل مع `from` و `to` (فلترة التاريخ)
- ✅ يعمل مع `branch` (فلترة الفرع)
- ✅ يعمل مع جميع معاملات الفلترة الأخرى

---

## 🎯 حالات الاستخدام (Use Cases)

### 1. تقارير تشغيلية شهرية

**السيناريو:** تريد تقرير المبيعات والمصروفات الشهرية بدون أرصدة افتتاحية

**الحل:**
```
GET /api/reports/sales-vs-expenses?from=2026-01-01&to=2026-01-31&exclude_opening=true
```

### 2. تحليل الأداء التشغيلي

**السيناريو:** تريد تحليل الأداء التشغيلي فقط (بدون أرصدة مرحلة)

**الحل:**
```
GET /api/reports/trial-balance?from=2026-01-01&to=2026-12-31&operational_only=true
```

### 3. كشف حساب تشغيلي

**السيناريو:** تريد كشف حساب بدون أرصدة افتتاحية

**الحل:**
```
GET /api/journal/account/123?from=2026-01-01&to=2026-12-31&exclude_opening=true
```

---

## ✅ الخلاصة (Summary)

### المميزات:

1. ✅ **فلترة اختيارية** - لا تؤثر على السلوك الافتراضي
2. ✅ **متوافقة مع جميع التقارير** - Trial Balance, Sales vs Expenses, Sales by Branch, Account Journal
3. ✅ **سهلة الاستخدام** - معامل واحد (`exclude_opening` أو `operational_only`)
4. ✅ **لا تؤثر على البيانات** - الفلترة فقط في الاستعلامات

### التوصيات:

1. **استخدم `exclude_opening=true`** عند الحاجة لتقارير تشغيلية فقط
2. **اترك المعامل بدون قيمة** عند الحاجة لجميع البيانات (بما في ذلك الأرصدة الافتتاحية)
3. **استخدم `operational_only=true`** كاسم بديل (نفس الوظيفة)

---

**تم إنشاء الدليل:** 2026-01-22  
**الحالة:** ✅ جاهز للاستخدام
