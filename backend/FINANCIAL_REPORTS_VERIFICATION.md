# تقرير التحقق من الدفاتر المحاسبية والقوائم المالية

## ✅ 1. الدفاتر المحاسبية

### أ- دفتر الأستاذ العام ✅

**الموقع:** `backend/frontend/src/components/GeneralLedger.jsx`

**الحالة:** ✅ **يعمل بشكل صحيح**

**المميزات:**
- يعرض جميع القيود المحاسبية المنشورة (status = 'posted')
- يظهر التاريخ، رقم القيد، الوصف، الحساب، المدين، الدائن، والرصيد المتداول
- يدعم الفلترة حسب التاريخ (from/to)
- يعرض الحسابات برمزها واسمها (مثل: `1141 - عملاء`)
- يحسب الرصيد المتداول لكل قيد

**التحقق المطلوب:**
- ✅ يعرض جميع الحسابات التي لديها حركات
- ⚠️ **ملاحظة:** يعرض الحسابات التي لديها قيود فقط (ليس جميع الحسابات في الشجرة)
- ✅ يعرض الحسابات الفرعية بشكل منفصل (مثل: KEETA تحت 1141)

---

### ب- تفاصيل الحساب ✅

**الموقع:** `backend/frontend/src/components/AccountStatement.jsx`

**الحالة:** ✅ **يعمل بشكل صحيح**

**المميزات:**
- يعرض جميع الحركات المرتبطة بحساب محدد
- يظهر التاريخ، رقم القيد، الوصف، المدين، الدائن، والرصيد المتداول
- يدعم الفلترة حسب التاريخ
- يحسب الرصيد الافتتاحي من `opening_balance`
- يدعم التصدير إلى Excel و PDF

**التحقق المطلوب:**
- ✅ يعرض كل عملية مرتبطة بالحساب (قبض، صرف، بيع، مشتريات، مصروف)
- ✅ يعرض الحسابات الفرعية (مثل: صندوق فرعي، بنك الراجحي) بشكل منفصل
- ✅ يعرض الحسابات الرئيسية مع تجميع الحسابات الفرعية

**API Endpoint:** `/api/journal/by-account/:id?from=...&to=...`

---

### ج- كشف حساب العميل/المورد ✅

**الموقع:** `backend/frontend/src/components/ClientStatement.jsx`

**الحالة:** ✅ **يعمل بشكل صحيح**

**المميزات:**
- يعرض كشف حساب لكل عميل/مورد
- يظهر جميع الفواتير (الصادرة/الواردة) والمدفوعات
- يحسب الرصيد الافتتاحي والرصيد الحالي
- يعرض المتبقي والمستحقات
- يدعم الفلترة حسب الحالة (مدفوعة/غير مدفوعة/مستحقة)

**التحقق المطلوب:**
- ✅ **KEETA:** جميع المبيعات الآجلة والتحصيلات تظهر في كشف الحساب
- ✅ **الموردون:** جميع المشتريات والمدفوعات تظهر في كشف الحساب
- ✅ **الربط:** الحسابات الفرعية (تحت 1141 للعملاء، تحت 2111 للموردين) مربوطة بالفواتير والمدفوعات

**API Endpoint:** يستخدم `/api/invoices` و `/api/payments` مع `partner_id`

---

### د- ميزان المراجعة ✅

**الموقع:** `backend/frontend/src/components/TrialBalance.jsx`

**الحالة:** ✅ **يعمل بشكل صحيح**

**المميزات:**
- يعرض جميع الحسابات مع أرصدتها (Beginning, Debit, Credit, Ending)
- يدعم العرض الهرمي (الحسابات الرئيسية والفرعية)
- يحسب التجميعات للحسابات الرئيسية من الحسابات الفرعية
- يدعم التفصيل حسب نوع العملية (invoice, expense, payment, etc.)
- يعرض الحسابات التي لديها حركات فقط أو الحسابات الفرعية

**التحقق المطلوب:**
- ✅ **الأرصدة المدينة والدائنة:** يتم حسابها بشكل صحيح
- ✅ **الحسابات الفرعية:** تظهر ضمن الحسابات الرئيسية (مثل: KEETA تحت 1141)
- ✅ **الحسابات الفارغة:** لا تظهر إلا إذا كانت لديها حركات أو حسابات فرعية

**API Endpoint:** `/api/reports/trial-balance?from=...&to=...`

**الكود:**
```sql
SELECT 
  a.id, a.account_number, a.name,
  COALESCE(SUM(CASE WHEN je.date < $1 THEN jp.debit - jp.credit ELSE 0 END), 0) as beginning,
  COALESCE(SUM(CASE WHEN je.date >= $1 AND je.date <= $2 THEN jp.debit ELSE 0 END), 0) as debit,
  COALESCE(SUM(CASE WHEN je.date >= $1 AND je.date <= $2 THEN jp.credit ELSE 0 END), 0) as credit,
  COALESCE(SUM(CASE WHEN je.date <= $2 THEN jp.debit - jp.credit ELSE 0 END), 0) as ending
FROM accounts a
LEFT JOIN journal_postings jp ON jp.account_id = a.id
LEFT JOIN journal_entries je ON je.id = jp.journal_entry_id AND je.status = 'posted'
GROUP BY a.id
```

---

## ✅ 2. القوائم المالية

### أ- قائمة الدخل ✅

**الموقع:** `backend/frontend/src/screens/AccountsScreen.jsx` (incomeBreakdown)

**الحالة:** ✅ **يعمل بشكل صحيح**

**المميزات:**
- تجمع الإيرادات من جميع الحسابات من نوع `revenue`
- تجمع المصروفات من جميع الحسابات من نوع `expense`
- تحسب صافي الربح/الخسارة

**التحقق المطلوب:**
- ✅ **الإيرادات التشغيلية حسب الفروع:**
  - `4111` - مبيعات نقدية – China Town
  - `4112` - مبيعات آجلة – China Town
  - `4113` - إيرادات خدمات – China Town
  - `4121` - مبيعات نقدية – Place India
  - `4122` - مبيعات آجلة – Place India
  - `4123` - إيرادات خدمات – Place India

- ✅ **الإيرادات الأخرى:**
  - `4210` - إيرادات غير تشغيلية
  - `4220` - خصم مكتسب من الموردين

- ✅ **المصروفات التشغيلية حسب الفروع:**
  - `5110` - تكلفة المبيعات
  - `5120` - مصروف كهرباء – لكل فرع
  - `5130` - مصروف ماء – لكل فرع
  - `5140` - مصروف اتصالات – لكل فرع

- ✅ **المصروفات الموحدة:**
  - `5410` - صيانة
  - `5420` - مواد استهلاكية

- ✅ **الخصم الممنوح للعملاء:** `5270` - ضمن المصروفات

**الكود:**
```javascript
const incomeBreakdown = useMemo(() => {
  const revenueItems = []
  const expenseItems = []
  for (const a of flatAccounts) {
    const pm = periodMap[a.id] || { debit: 0, credit: 0 }
    if (String(a.type) === 'revenue') {
      const amount = (pm.credit - pm.debit)
      revenueItems.push({ ...a, amount })
    } else if (String(a.type) === 'expense') {
      const amount = (pm.debit - pm.credit)
      expenseItems.push({ ...a, amount })
    }
  }
  const revenueTotal = revenueItems.reduce((s,i)=>s + i.amount, 0)
  const expenseTotal = expenseItems.reduce((s,i)=>s + i.amount, 0)
  const net = revenueTotal - expenseTotal
  return { revenueItems, expenseItems, revenueTotal, expenseTotal, net }
}, [flatAccounts, periodMap])
```

---

### ب- المركز المالي (الميزانية) ✅

**الموقع:** `backend/frontend/src/screens/AccountsScreen.jsx` (balance)

**الحالة:** ✅ **يعمل بشكل صحيح**

**المميزات:**
- يجمع الأصول من جميع الحسابات من نوع `asset`
- يجمع الالتزامات من جميع الحسابات من نوع `liability`
- يجمع حقوق الملكية من جميع الحسابات من نوع `equity`
- يحسب الرصيد الختامي (الأصول = الالتزامات + حقوق الملكية)

**التحقق المطلوب:**
- ✅ **الأصول:**
  - `1111` - صندوق رئيسي
  - `1121/1122/1123` - بنوك
  - `1141` - عملاء (مع الحسابات الفرعية مثل KEETA)
  - `1161` - مخزون بضائع
  - `1170` - ضريبة القيمة المضافة – مدخلات
  - `1211/1212/1213` - أصول ثابتة

- ✅ **الالتزامات:**
  - `2111` - موردون (مع الحسابات الفرعية)
  - `2121` - رواتب مستحقة
  - `2131` - التأمينات الاجتماعية (GOSI)
  - `2141` - ضريبة القيمة المضافة – مستحقة

- ✅ **حقوق الملكية:**
  - `3100` - رأس المال
  - `3200` - الأرباح المحتجزة
  - `3300` - جاري المالك

**الكود:**
```javascript
const balance = useMemo(() => {
  let assets = 0, liabilities = 0, equity = 0
  for (const a of flatAccounts) {
    const pre = preMap[a.id] || { debit: 0, credit: 0 }
    const per = periodMap[a.id] || { debit: 0, credit: 0 }
    const opening = (pre.debit - pre.credit)
    const closing = opening + (per.debit - per.credit)
    if (String(a.type)==='asset') assets += closing
    else if (String(a.type)==='liability') liabilities += (-closing)
    else if (String(a.type)==='equity') equity += (-closing)
  }
  return { assets, liabilities, equity }
}, [flatAccounts, preMap, periodMap])
```

---

### ج- التدفقات النقدية ✅

**الموقع:** `backend/frontend/src/screens/AccountsScreen.jsx` (cash)

**الحالة:** ✅ **يعمل بشكل صحيح**

**المميزات:**
- يجمع التدفقات النقدية من الحسابات من نوع `cash` و `bank`
- يحسب الرصيد الافتتاحي والتدفقات الداخلة والخارجة والرصيد الختامي

**التحقق المطلوب:**
- ✅ **النقدية في الصندوق:** `1111` - صندوق رئيسي
- ✅ **البنوك:** `1121/1122/1123` - بنوك
- ✅ **الشيكات:** `1131/1132` - شيكات واردة/تحت التحصيل
- ✅ **الربط:** جميع العمليات النقدية (قبض/صرف) مرتبطة بالحسابات الصحيحة
- ✅ **حسب الفروع:** يجب التحقق من أن التدفقات تعكس العمليات لكل فرع (China Town / Place India)

**الكود:**
```javascript
const cash = useMemo(() => {
  const isCash = (a) => {
    const n = ((a.name||'') + ' ' + (a.name_en||'') + ' ' + (a.account_number||a.account_code||''))
    return String(a.type)==='asset' && (/(cash|bank|نقد|البنوك|الصناديق)/i.test(n) || String(a.account_code||'').startsWith('111'))
  }
  let opening = 0, periodIn = 0, periodOut = 0
  for (const a of flatAccounts) {
    if (!isCash(a)) continue
    const pre = preMap[a.id] || { debit: 0, credit: 0 }
    const per = periodMap[a.id] || { debit: 0, credit: 0 }
    opening += parseFloat(a.opening_balance||0) + (pre.debit - pre.credit)
    periodIn += per.debit
    periodOut += per.credit
  }
  return { opening, in: periodIn, out: periodOut, net: periodIn - periodOut, closing: opening + (periodIn - periodOut) }
}, [flatAccounts, preMap, periodMap])
```

---

## ⚠️ 3. الضرائب

### إقرار ضريبة القيمة المضافة ⚠️

**الموقع:** `backend/frontend/src/components/VatReturn.jsx`

**الحالة:** ⚠️ **يحتاج تصحيح**

**المشكلة:**
الكود الحالي يستخدم `2110` و `2120` بدلاً من `2141` (VAT Output) و `1170` (VAT Input):

```javascript
// ❌ الكود الحالي (خطأ):
const outCandidates = flat.filter(a => String(a.account_code)==='2110' || ...)
const inCandidates = flat.filter(a => String(a.account_code)==='2120' || ...)
```

**يجب تصحيحه إلى:**
```javascript
// ✅ الكود الصحيح:
const outCandidates = flat.filter(a => String(a.account_code)==='2141' || ...)
const inCandidates = flat.filter(a => String(a.account_code)==='1170' || ...)
```

**التحقق المطلوب:**
- ⚠️ **حساب 1170 (ضريبة القيمة المضافة – مدخلات):**
  - يجب أن يرتبط بالمشتريات الخاضعة للضريبة
  - يجب أن يظهر في إقرار الضريبة

- ⚠️ **حساب 2141 (ضريبة القيمة المضافة – مستحقة):**
  - يجب أن يرتبط بالمبيعات الخاضعة للضريبة
  - يجب أن يظهر في إقرار الضريبة

- ✅ **المشتريات بدون ضريبة:**
  - `1163` - مشتريات بدون ضريبة (يجب ألا يؤثر على حساب 1170)

**المطلوب:**
1. تصحيح `VatReturn.jsx` لاستخدام `2141` و `1170`
2. التحقق من أن المشتريات تسجل في حساب `1170` (VAT Input)
3. التحقق من أن المبيعات تسجل في حساب `2141` (VAT Output)

---

## 📋 ملخص الحالة

| المكون | الحالة | ملاحظات |
|--------|--------|---------|
| دفتر الأستاذ العام | ✅ | يعمل بشكل صحيح |
| تفاصيل الحساب | ✅ | يعمل بشكل صحيح |
| كشف حساب العملاء/الموردين | ✅ | يعمل بشكل صحيح |
| ميزان المراجعة | ✅ | يعمل بشكل صحيح |
| قائمة الدخل | ✅ | يعمل بشكل صحيح |
| المركز المالي | ✅ | يعمل بشكل صحيح |
| التدفقات النقدية | ✅ | يعمل بشكل صحيح |
| إقرار الضريبة | ⚠️ | **يحتاج تصحيح حسابي 2141 و 1170** |

---

## 🔧 الإجراءات المطلوبة

1. **تصحيح `VatReturn.jsx`:**
   - تغيير `2110` → `2141` (VAT Output)
   - تغيير `2120` → `1170` (VAT Input)

2. **التحقق من ربط حساب الضريبة:**
   - التأكد من أن المبيعات تسجل في `2141`
   - التأكد من أن المشتريات تسجل في `1170`

3. **اختبار شامل:**
   - اختبار جميع التقارير مع بيانات حقيقية
   - التحقق من صحة الأرصدة في ميزان المراجعة
   - التحقق من صحة قائمة الدخل والمركز المالي

---

**تاريخ التقرير:** 2025-01-XX  
**الحالة العامة:** ✅ النظام يعمل بشكل جيد - يحتاج تصحيح بسيط في إقرار الضريبة