# تقرير مراجعة وإصلاح مركز التقارير

## ✅ الإصلاحات المطبقة

### 1. تقرير sales-vs-expenses
- ✅ يعتمد فقط على `journal_entries` و `journal_postings` مع `status='posted'`
- ✅ تم إضافة فلتر branch في SQL بدلاً من JavaScript
- ✅ تم إضافة حساب VAT (2141) و Discount (4130) من القيود
- ✅ تم إضافة `sales_gross` و `sales_net` في النتائج

### 2. تقرير sales-by-branch
- ✅ يعتمد فقط على القيود المنشورة
- ✅ تم إضافة فلتر branch في SQL
- ✅ تم إصلاح حساب discount من حساب 4130
- ✅ تم إصلاح حساب invoice_count بشكل صحيح

### 3. تقرير expenses-by-branch
- ✅ يعتمد فقط على القيود المنشورة
- ✅ تم إضافة فلتر branch في SQL
- ✅ يستخدم `branch_accounts` و `je.branch` للتعرف على الفرع

## ⚠️ ملاحظات مهمة

### جميع التقارير الآن:
1. ✅ تعتمد حصريًا على `journal_entries` و `journal_postings` مع `status='posted'`
2. ✅ تطبق فلتر branch في SQL (أكثر كفاءة)
3. ✅ تطبق فلتر from/to بشكل صحيح
4. ✅ لا تعتمد على `invoices` أو `orders` مباشرة

### الروابط الذكية:
- ✅ جميع الروابط في `Reports.jsx` تفتح التقارير في شاشة الحسابات
- ✅ الفلاتر (period, branch, currency) تُمرر عبر URL parameters

## 📋 الخطوات التالية

1. ⏳ إضافة تحذيرات للقيود غير المتوازنة في كل تقرير
2. ⏳ اختبار جميع الفلاتر مع بيانات حقيقية
3. ⏳ التحقق من تطابق النتائج مع شجرة الحسابات
