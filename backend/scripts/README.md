# سكريبتات النظام - دليل شامل

## 📋 نظرة عامة

هذا المجلد يحتوي على جميع السكريبتات المساعدة للنظام:

1. **مزامنة قاعدة البيانات** - `sync.js`
2. **اختبار API** - `test-api-endpoints.js`
3. **إضافة بيانات اختبارية** - `seed-test-data.js`
4. **التحقق من السلامة** - `check-integrity.js`
5. **مزامنة Sequelize** - `sync-sequelize.js`

---

## 🚀 السكريبتات المتاحة

### 1. `sync.js` - مزامنة قاعدة البيانات

**الوصف:** سكريبت ذكي لمزامنة قاعدة البيانات PostgreSQL
- يتحقق من وجود الجداول والأعمدة
- يضيف فقط ما هو مفقود
- لا يكرر الجداول أو الأعمدة الموجودة

**الاستخدام:**
```bash
node backend/scripts/sync.js
```

**الوثائق:** `SYNC_README.md`

---

### 2. `test-api-endpoints.js` - اختبار API شامل

**الوصف:** يختبر جميع API endpoints
- CRUD operations
- Posting operations
- Foreign Keys integrity

**الاستخدام:**
```bash
# تأكد من تشغيل الخادم أولاً
cd backend
npm start

# في terminal آخر
node backend/scripts/test-api-endpoints.js
```

**المتطلبات:**
- الخادم يجب أن يكون يعمل
- مستخدم admin موجود

**الوثائق:** `TESTING_GUIDE.md`

---

### 3. `seed-test-data.js` - إضافة بيانات اختبارية

**الوصف:** يضيف بيانات اختبارية للنظام
- حسابات محاسبية
- مصروفات تجريبية
- فواتير تجريبية
- قيود محاسبية

**الاستخدام:**
```bash
node backend/scripts/seed-test-data.js
```

**ما يضيفه:**
- 7 حسابات محاسبية أساسية
- 2 مصروفات تجريبية
- 1 فاتورة تجريبية
- 1 قيد محاسبي

---

### 4. `check-integrity.js` - التحقق من السلامة

**الوصف:** يتحقق من سلامة قاعدة البيانات
- Foreign Keys integrity
- Orphaned records
- Journal balance
- Missing relationships

**الاستخدام:**
```bash
node backend/scripts/check-integrity.js
```

**ما يتحقق منه:**
- ربط المصروفات بالقيود
- ربط الفواتير بالقيود
- توازن القيود (المدين = الدائن)
- السجلات المفقودة

---

### 5. `sync-sequelize.js` - مزامنة Sequelize

**الوصف:** مزامنة الجداول باستخدام Sequelize ORM

**الاستخدام:**
```bash
node backend/scripts/sync-sequelize.js
```

**الوثائق:** `SEQUELIZE_SETUP.md`

---

## 📊 سير العمل الموصى به

### عند إعداد النظام لأول مرة:

```bash
# 1. مزامنة قاعدة البيانات
node backend/scripts/sync.js

# 2. إضافة بيانات اختبارية
node backend/scripts/seed-test-data.js

# 3. التحقق من السلامة
node backend/scripts/check-integrity.js
```

### عند اختبار النظام:

```bash
# 1. شغّل الخادم
cd backend
npm start

# 2. في terminal آخر - اختبار API
node backend/scripts/test-api-endpoints.js

# 3. التحقق من السلامة
node backend/scripts/check-integrity.js
```

### عند إضافة جدول أو عمود جديد:

```bash
# 1. أضف التعريف في sync.js
# 2. شغّل المزامنة
node backend/scripts/sync.js

# 3. اختبر
node backend/scripts/test-api-endpoints.js
```

---

## 📚 الوثائق

| الملف | الوصف |
|-------|-------|
| `SYNC_README.md` | دليل استخدام sync.js |
| `TESTING_GUIDE.md` | دليل الاختبار الشامل |
| `SEQUELIZE_SETUP.md` | دليل إعداد Sequelize |
| `ACCOUNTING_FIX_README.md` | دليل إصلاح الربط المحاسبي |

---

## 🔧 استكشاف الأخطاء

### المشكلة: السكريبت لا يجد DATABASE_URL

**الحل:**
```bash
# تأكد من وجود ملف .env في مجلد backend
# أو حدد DATABASE_URL مباشرة:
DATABASE_URL=postgresql://... node backend/scripts/sync.js
```

### المشكلة: فشل تسجيل الدخول في test-api-endpoints.js

**الحل:**
```bash
# أنشئ مستخدم admin:
cd backend
node createAdmin.js
```

### المشكلة: Foreign Keys مكسورة

**الحل:**
```bash
# تحقق من السلامة:
node backend/scripts/check-integrity.js

# أصلح المشاكل يدوياً أو استخدم sync.js
node backend/scripts/sync.js
```

---

## ✅ Checklist

- [ ] `sync.js` يعمل بدون أخطاء
- [ ] `seed-test-data.js` يضيف البيانات بنجاح
- [ ] `test-api-endpoints.js` يمر بجميع الاختبارات
- [ ] `check-integrity.js` لا يجد مشاكل
- [ ] جميع الجداول متزامنة
- [ ] Foreign Keys تعمل بشكل صحيح

---

**تاريخ الإنشاء:** 2025-01-XX  
**آخر تحديث:** 2025-01-XX
