# E2E Playwright Test Script

## الوصف
سكريبت اختبار E2E كامل باستخدام Playwright لمحاكاة المستخدم الكامل في جميع وحدات النظام:
- المحاسبة والقيود اليومية
- العملاء
- الموردين
- الموظفين
- المنتجات
- نقطة البيع (POS)
- التقارير
- المصروفات

## المتطلبات

1. **تثبيت Playwright:**
```bash
npm install playwright --save-dev
npx playwright install chromium
```

2. **تأكد من تشغيل السيرفر:**
```bash
npm run dev
# أو
npm start
```

3. **تأكد من وجود متغيرات البيئة:**
- `DATABASE_URL` - رابط قاعدة البيانات PostgreSQL
- `ADMIN_EMAIL` - بريد المدير (افتراضي: admin@local.test)
- `ADMIN_PASSWORD` - كلمة مرور المدير (افتراضي: admin123)
- `API_BASE_URL` - رابط API (افتراضي: http://localhost:5000)

## الاستخدام

### تشغيل الاختبار في وضع Headless (بدون متصفح مرئي):
```bash
npm run test:e2e:playwright
```

### تشغيل الاختبار مع متصفح مرئي (للمراقبة):
```bash
HEADLESS=false npm run test:e2e:playwright
```

أو مباشرة:
```bash
node scripts/e2e-playwright-test.js
```

## ما يختبره السكريبت

1. **تسجيل الدخول:** تسجيل دخول المدير
2. **المحاسبة:** فتح صفحة القيود اليومية والتحقق من تحميلها
3. **العملاء:** فتح صفحة العملاء ومحاولة الوصول لصفحة الإنشاء
4. **الموردين:** فتح صفحة الموردين ومحاولة الوصول لصفحة الإنشاء
5. **الموظفين:** فتح صفحة الموظفين ومحاولة الوصول لصفحة الإنشاء
6. **المنتجات:** فتح صفحة المنتجات والتحقق من تحميلها
7. **نقطة البيع:** فتح صفحة POS ومحاولة اختيار فرع
8. **التقارير:** فتح صفحة التقارير والتحقق من الروابط
9. **المحاسبة:** فتح شاشة المحاسبة والتحقق من تحميلها
10. **المصروفات:** فتح صفحة المصروفات والتحقق من تحميلها

## المخرجات

- ✅ رسائل نجاح لكل وحدة تم اختبارها
- ❌ رسائل خطأ مع تفاصيل في حالة الفشل
- 📸 لقطة شاشة كاملة في حالة الفشل (محفوظة في `test-screenshot.png`)

## ملاحظات

- السكريبت يستخدم تنفيذ صارم: أي خطأ يوقف السكريبت مباشرة
- يتم إنشاء/تحديث مستخدم المدير تلقائياً قبل الاختبار
- السكريبت ينتظر تحميل الصفحات بشكل كامل قبل المتابعة
- يمكن تعديل `slowMo` في الكود لإبطاء التنفيذ للمراقبة

## استكشاف الأخطاء

إذا فشل الاختبار:
1. تأكد من أن السيرفر يعمل على المنفذ الصحيح
2. تحقق من أن قاعدة البيانات متاحة
3. راجع لقطة الشاشة المحفوظة (`test-screenshot.png`)
4. تأكد من أن المستخدم المدير موجود وله الصلاحيات الكافية
