# 🚀 تعليمات تشغيل الخوادم في وضع المطور

## ✅ تم تشغيل الخوادم

### Backend Server
- **المنفذ:** 4000
- **URL:** `http://localhost:4000`
- **API:** `http://localhost:4000/api`
- **الحالة:** ✅ يعمل

### Frontend Dev Server (اختياري)
- **المنفذ:** 3000
- **URL:** `http://localhost:3000`
- **الحالة:** ⚠️ قد يحتاج تشغيل يدوي

---

## 📋 كيفية الوصول

### الطريقة 1: استخدام Backend Server (مُوصى به)

**URL:** `http://localhost:4000`

هذا الخادم يخدم:
- ✅ API endpoints (`/api/*`)
- ✅ Frontend build (من `backend/frontend/build`)
- ✅ جميع المسارات

**مميزات:**
- يعمل على منفذ واحد
- لا يحتاج تشغيل منفصل للـ frontend
- التغييرات في الكود تحتاج rebuild

### الطريقة 2: استخدام Frontend Dev Server (للتطوير السريع)

**لتشغيل Frontend Dev Server:**

```bash
cd backend/frontend
npm start
```

**URL:** `http://localhost:3000`

**مميزات:**
- ✅ Hot reload - التغييرات تظهر فوراً
- ✅ لا يحتاج rebuild
- ⚠️ يحتاج Backend server يعمل على 4000

---

## 🔄 Hot Reload في وضع المطور

### للتطوير السريع:

1. **شغّل Backend Server:**
   ```bash
   cd backend
   npm start
   ```

2. **في terminal آخر، شغّل Frontend Dev Server:**
   ```bash
   cd backend/frontend
   npm start
   ```

3. **افتح المتصفح على:**
   ```
   http://localhost:3000
   ```

**الآن:**
- ✅ أي تغيير في `backend/frontend/src` سيظهر فوراً
- ✅ Backend API متاح على `http://localhost:4000/api`
- ✅ Frontend Dev Server يتصل تلقائياً بـ Backend

---

## 📊 الحالة الحالية

| الخادم | المنفذ | الحالة | URL |
|--------|--------|--------|-----|
| Backend | 4000 | ✅ يعمل | http://localhost:4000 |
| Frontend Dev | 3000 | ⚠️ اختياري | http://localhost:3000 |

---

## ⚡ ملاحظات

1. **Backend Server** يعمل الآن على `http://localhost:4000`
2. **Frontend Dev Server** اختياري - للتطوير السريع فقط
3. **التغييرات في POSInvoice.jsx** تحتاج:
   - إما rebuild: `cd backend/frontend && npm run build`
   - أو استخدام Frontend Dev Server للحصول على hot reload

---

**جاهز للتطوير!** 🎉
